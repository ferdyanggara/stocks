import React, { Component } from 'react';

export class Input extends Component {
  render() {
    return (
      <div>
        <input type='text' placeholder='enter stocks name ' />
      </div>
    );
  }
}

export default Input;
